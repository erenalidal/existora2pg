oracle:
  dsn: "oracle://migtest:migtest123@localhost:1521/FREEPDB1"
  schema: "MIGTEST"
  max_conns: 4

postgres:
  dsn: "postgres://migtest:migtest123@localhost:5434/migtest?sslmode=disable"
  schema: "public"
  max_conns: 4
  unlogged: true

migration:
  workers: 4
  batch_size: 50000
  fetch_size: 5000
  state_db_path: "./migration_test_state.db"
  validate_after: true
  naming_convention: "lowercase"

logging:
  level: "info"
  format: "text"
